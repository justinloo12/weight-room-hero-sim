# Secrets — never commit real credentials
.env

# Large Statcast datasets (hundreds of MB — regenerate with pull_data.py /
# build_features.py). Anything matching homerun_data_*.csv stays local.
homerun_data_*.csv
props.csv

# Holdout evaluation inputs (regenerate: see evaluate_model.py docstring)
holdout_2026.csv
holdout_weather.csv

# Local operational log of Odds API credit burn (machine-specific)
odds_quota_log.csv

# Python
__pycache__/
*.pyc
.venv/

# OS junk
.DS_Store
